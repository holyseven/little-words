# 仅在本地代理地址不是默认 /api/pronunciation 时填写公开 URL。
# 不要把 Azure 密钥写入 VITE_* 变量，它们会进入网页包。
VITE_PRONUNCIATION_PROXY_URL=

# 复制为 .env.local（已被 git 忽略）后填写，Vite 本地代理会读取它们。
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastus
# 可选：使用自定义 Azure Speech endpoint；留空则按 region 拼接。
AZURE_SPEECH_ENDPOINT=
